{
  "name": "cuttings",
  "version": "0.4.0",
  "private": false,
  "description": "Saved searches that catch up automatically, with visible exact checks and optional semantic evaluation.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "5.0.4",
    "vite": "5.4.21",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
